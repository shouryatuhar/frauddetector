# Demo dependencies (extends requirements.txt)
# Install with: pip install -r demo_requirements.txt

# All ML dependencies from the original project
-r requirements.txt

# Web server
flask>=3.0,<4.0
gunicorn>=22.0,<24.0
